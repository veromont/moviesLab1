@model IEnumerable<moviesAPI.Models.EntityModels.Session>

@{
    ViewData["Title"] = "Index";
}

<h2>Перелік сеансів:</h2>

@if (Model.Count() == 0)
{
    <h2 style="text-align:center;">Тут порожньо</h2>
}
else
{
    <hr />
            <>Search/filters here</>
    <hr />
    await Html.RenderPartialAsync("_PartialTable", Model);
}
